sudo: required
language: generic 

services:
  - docker

before_install:
  - docker build -t peetur/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true peetur/docker-react npm run test -- --coverage